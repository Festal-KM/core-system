import { Typography, Box } from '@mui/material';

const RevitalizationPage = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        地方創生事業部
      </Typography>
      <Typography variant="body1">
        地方創生事業部のコンテンツはここに表示されます。
      </Typography>
    </Box>
  );
};

export default RevitalizationPage; 